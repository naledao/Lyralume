# 音乐分析与动态视觉系统开发指导

## 1. 建设目标

将现有基础频谱和固定粒子轨道升级为具有歌曲身份、段落变化和实时响应的动态视觉系统，建立以下闭环：

```text
整曲分析与缓存 → 生成歌曲视觉画像 → 实时音频驱动 → 段落与节拍编排 → 程序化粒子渲染
```

系统应保证同一歌曲的视觉风格稳定，不同歌曲具有明显差异，并且视觉分析、渲染或缓存异常均不影响音频播放。

## 2. 总体方案

采用“离线整曲分析 + 实时分析 + Visual DNA”的混合架构：

- 离线分析负责歌曲的整体响度、频谱分布、节奏、调性和段落结构。
- 实时分析负责当前播放位置的能量、频带、起音和瞬态变化。
- Visual DNA 将歌曲画像映射为稳定的形态、粒子、力场和运动参数。
- 视觉运行时合并 Visual DNA、当前段落、节拍事件和实时特征，驱动 Canvas 2D 渲染。
- Canvas 2D 不能满足沉浸模式性能目标时，才增加可选 WebGL2 后端。

## 3. 模块与进程边界

### Electron 主进程

- 负责分析任务调度、文件权限、缓存状态和 SQLite 持久化。
- 通过独立 Utility Process 执行整曲分析，禁止在主进程内运行重计算。
- 使用 FFmpeg 只读解码音频到内存或标准输出，不创建替代音频文件。
- 只通过最小化、类型化 IPC 向渲染进程提供歌曲画像、时间轴和任务状态。

### 渲染进程

- 音频输出与分析旁路必须分离，关闭分析或视觉不能中断播放。
- 使用 AudioWorklet 或独立分析节点采集实时音频数据。
- 频繁的实时特征只在渲染进程内部传递，不通过 Electron IPC 逐帧发送。
- React 组件只负责视觉容器、设置和生命周期，不直接承担完整粒子模拟。

### 共享分析核心

- 实时分析与整曲分析复用同一套 TypeScript DSP 实现和特征定义。
- 提供频谱、响度、频带、频谱质心、平坦度、Flux、起音、节拍、调性和段落等结果。
- 所有算法输出必须包含版本号，并能够通过合成测试音频验证。

## 4. Visual DNA 与视觉引擎

Visual DNA 应由歌曲分析结果确定，而不是通过曲风标签直接选择固定预设。建议使用能量、节奏、明亮度、纹理、调性、动态、空间感和复杂度等连续视觉轴进行映射。

视觉引擎由以下部分组合：

- 形态：Superformula、螺旋、Lissajous、波带、流场、放射碎片和星座网络等。
- 发射器：曲线发射、中心爆发、频带定向和节拍冲击。
- 力场：轨道吸引、旋转、径向冲击、阻尼和噪声流场。
- 粒子样式：光点、线段、碎片、尘埃和丝带。
- 合成效果：辉光、拖尾、背景光和分层透明度。

同一歌曲应使用持久化的确定性种子。切歌或重启后，Visual DNA 应保持一致；段落变化只在同一视觉身份内平滑变形。

## 5. 数据与缓存

为歌曲分析建立独立 SQLite 记录，至少保存：

- 分析状态、进度和错误。
- 分析算法版本与视觉映射版本。
- 文件大小、修改时间和内容指纹。
- 全曲画像、节拍时间、段落信息和 Visual DNA。
- 最近一次成功的分析结果。

文件或分析算法变化后应将结果标记为过期，并在后台重新分析。重新分析失败时保留上一次可用结果；未完成分析的歌曲必须能够退回实时视觉模式。

## 6. 建议开发顺序

### 第一步：建立共享分析底座

拆分播放输出和分析旁路，完成共享 DSP 接口、实时特征流和合成音频测试。

### 第二步：完成 Visual DNA 垂直闭环

实现确定性种子、连续视觉轴和首批程序化形态，先验证不同歌曲能够形成稳定差异。

### 第三步：接入整曲分析与缓存

建立 Utility Process、FFmpeg 只读解码、任务状态、SQLite 缓存和失败恢复。

### 第四步：加入节拍、调性和段落

生成可查询的节拍与段落时间轴，支持播放、暂停、切歌和跳转后的重新同步。

### 第五步：完善视觉引擎

扩充形态、发射器、力场和粒子样式，完成形态插值、封面配色融合和沉浸模式编排。

### 第六步：性能与产品化

加入质量档位、自适应降级、减少动态、闪烁限制、缓存清理、日志和完整测试。

## 7. 性能与安全要求

- 分析任务默认低并发运行，播放和界面响应优先。
- 粒子模拟使用 TypedArray、对象复用和预计算，避免逐帧大量分配。
- 根据帧耗时动态降低辉光、拖尾、粒子数、分辨率或帧率。
- 窗口隐藏、最小化或视觉关闭时停止不必要的绘制和分析推送。
- 提供减少动态和视觉强度设置，避免高频、大面积或高亮度闪烁。
- 外部进程必须使用参数数组启动，不得拼接 Shell 命令。
- 分析过程不得修改、覆盖、重新编码或写入用户原始音乐。

## 8. 依赖与许可证

- FFT 可采用小型、宽松许可证的实现，并通过 lockfile 精确锁定。
- FFmpeg 必须使用经过审计的 LGPL 构建，记录来源、版本、构建配置、许可证和文件哈希。
- 默认不将 Essentia.js 等强 copyleft 依赖并入核心应用。
- 情绪、流派或乐器模型只能作为可选资源包，完成许可证和模型来源审查后再接入。

## 9. 交付物

- 可复用于实时与离线场景的音频分析核心。
- 可恢复的整曲分析服务和 SQLite 缓存。
- 稳定、可版本化的 Visual DNA 映射。
- 可组合的程序化粒子与形态引擎。
- 视觉质量、强度和减少动态设置。
- 分析、映射、渲染、性能和 Electron 端到端测试。

## 10. 完成标准

- 同一歌曲跨切歌和应用重启后保持一致的视觉身份。
- 不同声音特征的歌曲能够产生可辨识的形态和运动差异。
- 视觉能跟随实时频谱、起音、节拍和段落变化。
- 暂停、跳转和快速切歌后不存在上一首歌曲的分析或粒子残留。
- 整曲分析失败、视觉关闭或视觉模块异常时，音频仍能正常播放。
- 分析不会修改用户原始音乐，任务状态和最近一次成功结果可恢复。
- 平衡质量档在项目指定的 Win11 x64 基准设备上达到稳定的沉浸模式目标帧率。
- 所有新增依赖、二进制和可选模型均具有完整的版本、许可证和哈希记录。
