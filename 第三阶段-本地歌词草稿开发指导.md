# 第三阶段：本地歌词草稿开发指导

## 1. 本阶段目标

在在线歌词无结果时，建立本地歌词草稿处理闭环：

```text
原始音乐 → 分离临时人声 → 转写与对齐 → 生成歌词草稿 → 用户校对 → 确认保存与嵌入
```

自动生成的内容只能作为草稿。未经用户确认，不得覆盖已有 LRC 或写入音乐标签。

## 2. 实施范围

- 建立独立的 UVR Worker 和 WhisperX Worker。
- 使用 Audio Separator/UVR 生成人声中间文件。
- 使用 WhisperX 生成原始转写和词/字级对齐结果。
- 保存任务清单、原始转写、对齐 JSON 和 LRC 草稿。
- 提供文本修改、拆行、合行、时间偏移和低置信度提示。
- 用户确认后复用现有 LRC 安全保存与 Kid3 写入流程。

模型下载管理、完整任务恢复和缓存治理留到第四阶段完善。

## 3. 建议开发顺序

### 第一步：定义任务与数据协议

为每次生成建立任务 ID、阶段状态和独立缓存目录。Electron 与 Worker 使用 stdin/stdout 上的 JSON Lines 通信，并在消息和日志中携带任务 ID。

### 第二步：接入人声分离 Worker

使用独立、锁定版本的 Python 环境运行 Audio Separator/UVR。输出临时 WAV 或 FLAC 人声文件，保持原曲时间轴，不修改原始音乐。

### 第三步：接入 WhisperX Worker

在另一个独立 Python 环境中完成转写和强制对齐，保留原始结果、对齐时间和置信度。UVR 释放资源后再启动 WhisperX，GPU 任务默认串行。

### 第四步：生成 LRC 草稿

以对齐 JSON 作为内部主数据，将词或字符合并为适合显示的歌词行，并生成 UTF-8 LRC 草稿。缺少时间或置信度较低的内容必须明确标记。

### 第五步：完成歌词校对页面

支持原曲与临时人声试听、当前行循环、文本修改、拆行、合行和整体时间偏移。用户可以保存草稿，但只有明确确认后才能保存正式 LRC 或嵌入标签。

### 第六步：处理失败与取消

Worker 必须作为外部进程启动，并使用参数数组传递路径。任务失败或取消时保留已完成阶段的中间结果，单个 Worker 崩溃不得影响播放器。

## 4. 本阶段交付物

- 可被 Electron 调度的 UVR 与 WhisperX Worker。
- 包含任务清单、原始转写、对齐 JSON 和 LRC 草稿的任务目录。
- 可查看和修改自动识别结果的歌词校对页面。
- 用户确认后可安全保存并嵌入歌词的完整流程。
- 针对协议、歌词编译、失败处理和关键界面的测试。

## 5. 完成标准

- 在线无结果时，用户能够启动本地歌词草稿任务。
- UVR 与 WhisperX 环境相互隔离，GPU 阶段不会并行争用资源。
- 原始音乐不会被修改或重新编码，中间文件只保存在应用缓存目录。
- 应用能够展示进度、错误和低置信度内容。
- 用户能够校对草稿，并在确认后保存 LRC 和写入标签。
- 任一阶段失败时，已有歌词、草稿和可复用中间结果不会丢失。

达到以上标准后，再进入第四阶段：模型管理、任务恢复、缓存清理、纯文本歌词重新对齐和扩大测试覆盖。
