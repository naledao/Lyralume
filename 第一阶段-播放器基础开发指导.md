# 第一阶段：播放器基础开发指导

## 1. 下一步目标

当前项目仍处于设计阶段，下一步应先完成可运行的播放器基础版，建立以下最小闭环：

```text
导入音乐目录 → 建立音乐库 → 播放原文件 → 同步显示 LRC → 驱动基础视觉效果
```

本阶段只验证本地播放器的核心架构，不接入 LRCLIB、Kid3、UVR、WhisperX、CUDA 或模型下载。

## 2. 实施范围

- 使用 Electron、React、TypeScript、Vite 和 pnpm 建立项目骨架。
- 通过 Electron 主进程扫描音乐文件，使用 `music-metadata` 读取元数据，并用 SQLite 保存音乐库。
- 使用 `HTMLAudioElement` 和 Web Audio API 完成原文件播放、进度、音量、切歌和音频分析。
- 实现项目内的 LRC 解析、同步高亮、滚动和整体偏移。
- 使用 Zustand 管理播放、队列、当前歌曲和歌词状态。
- 使用 GSAP 与 Canvas 2D 实现一套基础音乐响应式视觉效果。
- 建立必要的日志、异常隔离和基础测试。

## 3. 建议开发顺序

### 第一步：创建安全的桌面应用骨架

建立主进程、预加载脚本和 React 渲染进程。关闭 `nodeIntegration`、启用上下文隔离，并只通过最小化的类型化 IPC 暴露本地能力。

### 第二步：完成本地音乐库

支持选择和记忆音乐目录，扫描文件、读取元数据并写入 SQLite。渲染进程只消费主进程返回的数据，不直接访问文件系统或数据库。

### 第三步：完成播放器闭环

实现歌曲加载、播放暂停、切歌、进度跳转、音量控制和资源释放。播放过程中不得修改或重新编码原始音乐。

### 第四步：完成本地歌词显示

读取同名 LRC，解析时间戳并随播放进度高亮和滚动。无歌词或歌词损坏时，应显示明确状态且不影响播放。

### 第五步：加入基础视觉效果

从 Web Audio API 获取音量、时域和频域数据，驱动独立的 Canvas 视觉模块。视觉效果关闭或异常时，音频仍须正常播放。

### 第六步：验证稳定性

覆盖空目录、损坏文件、元数据缺失、无歌词、快速切歌和应用重启等场景。音频格式兼容性以真实测试样本为准。

## 4. 本阶段交付物

- 可启动和正常退出的 Win11 桌面应用。
- 可扫描、保存并展示歌曲信息的本地音乐库。
- 可连续播放和切换本地原文件的播放器。
- 可同步显示同名 LRC 的歌词区域。
- 可独立关闭的基础音乐响应式视觉效果。
- 针对核心模块的单元测试和基础 Electron 端到端测试。

## 5. 完成标准

- “导入、选择、播放、歌词、视觉”流程可以连续运行。
- 播放进度、歌词高亮和视觉效果保持基本同步。
- 单个文件或视觉模块异常不会导致播放器整体退出。
- 主进程、预加载脚本和渲染进程的职责边界清晰。
- 原始音乐内容未被修改，切歌和退出时资源得到释放。

达到以上标准后，再编写并进入第二阶段指导：LRCLIB 在线歌词查询、候选确认、LRC 原子保存和 Kid3 写入验证。
