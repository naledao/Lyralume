# Win11 本地音乐播放器开发指导

## 1. 文档目的

本文用于指导一个 Win11 本地音乐播放器的设计与开发。播放器复用 Mineradio 的音乐响应式视觉效果，并集成两条歌词处理路线：

1. `LRCLIB/LRCGET → LRC → Kid3`
2. `UVR → WhisperX → LRC → Kid3`

本文侧重产品结构、模块边界和开发顺序，不展开模型调参、底层音频算法及具体界面实现。

### 1.1 当前进度

截至 2026 年 7 月，第一阶段“播放器基础”和第二阶段“在线歌词”已经完成。项目已具备本地音乐库、原文件播放、同名 LRC 同步显示、音乐响应式视觉效果、LRCLIB 候选确认、安全 LRC 保存，以及 Kid3 写入与回读验证能力。

当前进入第三阶段“本地歌词草稿”。本阶段将接入独立的 UVR 与 WhisperX Worker，保存转写和对齐结果，生成可校对的歌词草稿。模型管理、任务恢复完善和缓存清理仍留在第四阶段。

## 2. 产品目标

- 播放用户本地音乐文件。
- 提供基于播放音频的动态视觉效果。
- 优先从在线歌词库获取同步歌词。
- 在线歌词不可用时，在本地从音乐中生成歌词草稿。
- 将歌词保存为同名 LRC，并可嵌入音乐文件元数据。
- 歌词生成和嵌入过程不得重新编码原始音乐。
- AI 模型完成首次下载后，允许离线执行本地识别路线。

## 3. 范围约束

### 3.1 Mineradio 使用范围

只借鉴或复用 Mineradio 的音乐响应式视觉效果，包括频谱、粒子、背景动画及与音频分析数据的联动。

不沿用其在线电台、音源搜索和流媒体音质选择逻辑。本地音乐始终从原文件直接播放，视觉模块只读取分析数据，不修改音频。

### 3.2 歌词处理原则

- 已有同名 LRC 时优先使用。
- 不自动覆盖用户已有歌词。
- 在线匹配结果存在歧义时要求用户确认。
- AI 识别结果默认标记为“歌词草稿”。
- 第一版必须经过用户校对后才能嵌入。
- LRC 是对外兼容文件，带置信度和词级时间的 JSON 是内部主数据。

## 4. 总体架构

```text
桌面播放器
├─ 音乐库与元数据模块
├─ 本地播放模块
├─ Mineradio 视觉效果模块
├─ 歌词任务调度器
│  ├─ LRCLIB 客户端
│  ├─ UVR 人声分离 Worker
│  ├─ WhisperX 识别 Worker
│  └─ 歌词编译与校对模块
├─ Kid3 标签写入适配器
├─ 模型与缓存管理器
└─ 本地数据库
```

播放器主程序负责界面、播放和任务管理。UVR、WhisperX 与 Kid3 应作为独立后台进程运行，避免 Python、CUDA 和音频依赖影响主程序稳定性。

## 5. 歌词路线选择

建议按照以下顺序处理每首歌曲：

```text
已有同名 LRC
  → 已有嵌入歌词
  → LRCLIB 同步歌词
  → LRCLIB 纯文本歌词 + 本地对齐
  → UVR + WhisperX 自动转写
  → 无结果或人工编辑
```

### 5.1 在线匹配路线

```text
读取标题、歌手、专辑和时长
  → 查询 LRCLIB
  → 自动采用高可信结果或展示候选
  → 保存同名 LRC
  → Kid3 嵌入 SYLT
  → 回读验证
```

这条路线速度快，应作为默认方案。LRCGET 主要作为 LRCLIB 交互和歌词导出逻辑的参考实现；正式产品直接集成 LRCLIB API，不自动操作 LRCGET 图形界面。

### 5.2 本地生成路线

```text
原始音乐
  → UVR 模型提取临时人声文件
  → WhisperX 转写并生成词/字级时间
  → 歌词编译器生成 aligned.json 和 LRC
  → 用户试听校对
  → Kid3 嵌入 SYLT
  → 回读验证
```

UVR 阶段建议使用支持 UVR 模型的 Audio Separator CLI 或 Python API。中间人声文件使用 WAV 或 FLAC，并保持与原曲相同的时间轴。

WhisperX 负责转写和强制对齐。歌曲中的和声、混合语言、长音及较强混响可能降低准确率，因此自动生成结果不能直接视为最终歌词。

## 6. 核心模块职责

### 6.1 音乐库模块

- 扫描本地音乐文件。
- 读取文件路径、标题、歌手、专辑和时长。
- 检测同名 LRC 与嵌入歌词。
- 监听标签写入后的文件变化。

### 6.2 播放与视觉模块

- 播放本地原始音频。
- 输出当前时间、播放状态和音频分析数据。
- 向视觉效果提供频域、时域和音量数据。
- 不参与歌词生成和标签写入。

### 6.3 歌词任务调度器

- 选择在线匹配或本地生成路线。
- 管理排队、暂停、取消、恢复和失败重试。
- 同一音乐文件不得同时执行多个写入任务。
- GPU 推理任务默认串行执行。

### 6.4 歌词编译与编辑模块

- 保存 WhisperX 的原始和对齐结果。
- 将词或字符时间合并成适合显示的歌词行。
- 生成 UTF-8 LRC。
- 提供文本纠错、拆行、合并和整体时间偏移。
- 标记缺少时间戳或低置信度的内容。

### 6.5 标签写入适配器

- 调用 `kid3-cli` 将 LRC 写入 ID3v2 `SYLT`。
- 在写入当前播放文件前暂停并释放文件。
- 保存播放位置，写入后重新打开并恢复播放位置。
- 检查退出码，并通过回读验证写入结果。
- 写入失败时保留 LRC 和草稿数据。

## 7. 数据与文件组织

每首歌曲建议记录以下状态：

- 文件绝对路径和文件指纹。
- 基础音乐元数据。
- 歌词来源与处理状态。
- LRCLIB 记录 ID（如有）。
- 分离模型、识别模型和语言。
- 草稿、已确认及已嵌入状态。
- 最近一次错误信息。

临时任务目录示例：

```text
cache/jobs/{job-id}/
├─ manifest.json
├─ vocals.flac
├─ transcript.raw.json
├─ transcript.aligned.json
└─ lyrics.draft.lrc
```

确认后的 LRC 保存到音乐文件旁边。中间人声和模型输出位于应用缓存目录，并允许用户统一清理。

## 8. Windows 部署策略

- 主程序与 AI 运行环境分离。
- UVR Worker 和 WhisperX Worker 使用各自锁定版本的 Python 环境。
- 两个 Worker 不同时占用 GPU，前一阶段退出后再启动下一阶段。
- 优先正式支持 NVIDIA CUDA；CPU 作为兼容模式；AMD/Intel GPU 初期标记为实验功能。
- AI 组件和模型作为可选资源包按需下载，避免核心安装包过大。
- 所有模型记录版本、来源、许可证和文件哈希。
- FFmpeg、Kid3 及其运行库应随对应组件完整分发或由安装器明确检测。
- 后台推理使用较低进程优先级，减少对音乐播放和视觉效果的影响。

## 9. 用户界面建议

播放器至少提供以下歌词状态：

- 未检查
- 正在查找在线歌词
- 等待候选确认
- 正在分离人声
- 正在识别与对齐
- 等待校对
- 正在嵌入
- 已完成
- 无结果或失败

歌词校对页应支持：

- 原曲与临时人声切换试听。
- 当前歌词行循环播放。
- 文本修改和歌词行调整。
- 时间整体偏移。
- 低置信度内容提示。
- 保存草稿与确认嵌入。

## 10. 安全与可靠性

- 不直接修改或替换原始音频内容。
- 写入 LRC 时先写临时文件，再进行原子替换。
- 首次修改标签前提供备份选项。
- 调用外部进程时使用参数数组，不拼接 Shell 命令。
- 写标签期间对目标文件加任务锁。
- 应用异常退出后可从任务清单恢复未完成任务。
- 模型下载失败、显存不足或进程崩溃时，应保留已有中间结果。

## 11. 开发阶段

### 第一阶段：播放器基础（已完成）

- 完成本地音乐扫描和播放。
- 接入 Mineradio 风格的音乐视觉效果。
- 支持读取和显示同名 LRC。
- 详细范围参见 `第一阶段-播放器基础开发指导.md`。

### 第二阶段：在线歌词（已完成）

- 接入 LRCLIB 查询与候选选择。
- 安全保存同名 LRC，不自动覆盖已有歌词。
- 调用 Kid3 写入及回读验证。
- 详细范围参见 `第二阶段-在线歌词开发指导.md`。

### 第三阶段：本地歌词草稿（下一步）

- 接入 Audio Separator 人声分离。
- 接入 WhisperX 转写和对齐。
- 完成 aligned JSON 到 LRC 的转换。
- 提供基础歌词校对页。
- 详细范围参见 `第三阶段-本地歌词草稿开发指导.md`。

### 第四阶段：工程化完善（未开始）

- 增加模型管理、任务恢复和缓存清理。
- 增加纯文本歌词重新对齐。
- 建立不同语言、曲风和硬件的测试集。
- 根据测试结果决定是否开放自动嵌入。

## 12. 完整第一版验收标准

第一、第二阶段已经完成；以下标准中的本地歌词草稿、任务恢复等内容仍需在第三、第四阶段实现：

- 本地音乐可稳定播放，视觉效果与音频同步。
- 已有 LRC 可正确显示和滚动。
- LRCLIB 匹配、候选确认、保存和嵌入流程完整。
- 在线无结果时可生成本地歌词草稿。
- 用户能够修改草稿并写入音乐文件。
- 标签写入前后音频内容未被重新编码。
- 失败任务不会破坏原音乐或覆盖已有歌词。
- 应用重启后可以识别已完成和未完成的歌词任务。

## 13. 许可证注意事项

- Mineradio：GPL-3.0，直接复用代码时需要评估整个应用的开源义务。
- LRCGET：MIT。
- UVR 与 Audio Separator：MIT，使用模型时保留项目和模型作者署名。
- WhisperX：BSD-2-Clause。
- Whisper：MIT。
- Kid3：GPL，随应用分发时需要满足其许可证要求。
- 不同分离和对齐模型可能使用不同许可证，发布前必须逐个核对。

本节仅用于开发管理，不构成法律意见。

## 14. 主要参考资料

- [Mineradio](https://github.com/XxHuberrr/Mineradio)
- [LRCGET](https://github.com/tranxuanthang/lrcget)
- [LRCLIB API](https://www.lrclib.net/docs)
- [Ultimate Vocal Remover](https://github.com/Anjok07/ultimatevocalremovergui)
- [Audio Separator](https://github.com/nomadkaraoke/python-audio-separator)
- [WhisperX](https://github.com/m-bain/whisperX)
- [Whisper](https://github.com/openai/whisper)
- [Kid3](https://kid3.kde.org/)
