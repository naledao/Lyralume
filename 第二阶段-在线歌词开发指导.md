# 第二阶段：在线歌词开发指导

## 1. 本阶段目标

在现有本地播放器上建立在线歌词处理闭环：

```text
读取歌曲信息 → 查询 LRCLIB → 确认匹配结果 → 保存同名 LRC → 写入标签 → 回读验证
```

本阶段只处理在线歌词及标签写入，不接入 UVR、WhisperX、CUDA 或模型下载。

## 2. 实施范围

- 在 Electron 主进程中封装 LRCLIB REST API 客户端。
- 使用标题、歌手、专辑和时长查询同步歌词。
- 对候选结果进行基础评分；存在歧义时由用户确认。
- 将确认后的同步歌词保存为 UTF-8 同名 LRC。
- 通过 `kid3-cli` 写入同步歌词，并在写入后回读验证。
- 记录歌词来源、候选状态、保存状态、写入结果和错误信息。

LRCGET 仅作为交互和实现参考，不通过自动化操作其图形界面。

## 3. 建议开发顺序

### 第一步：扩展歌词状态与 IPC

补充在线查询、等待确认、保存、写入、完成和失败等状态。渲染进程只发起操作和展示结果，网络请求、文件写入及外部进程调用均由主进程负责。

### 第二步：接入 LRCLIB 查询

根据当前歌曲元数据查询同步歌词，并将结果转换为统一的候选数据。网络错误、无结果和缺失同步歌词需要有明确状态。

### 第三步：完成候选确认

高可信且无歧义的结果可以简化确认流程；多个相近结果必须展示标题、歌手、专辑、时长和歌词预览，由用户选择。

### 第四步：安全保存 LRC

保存前检查同名 LRC。已有歌词时不得自动覆盖；用户确认保存后，先写临时文件，再原子替换目标文件。

### 第五步：接入 Kid3

使用参数数组启动 `kid3-cli`。写入前暂停目标歌曲、保存播放位置、释放文件句柄并加任务锁；写入后回读验证，再恢复歌曲和播放位置。

### 第六步：补充测试与故障处理

覆盖无网络、无匹配、候选歧义、已有 LRC、无 Kid3、写入失败和回读不一致等场景。任何失败都必须保留已有 LRC 和可复用结果。

## 4. 本阶段交付物

- 可查询 LRCLIB 并展示候选的在线歌词功能。
- 可确认、保存且不会静默覆盖已有文件的 LRC 流程。
- 可写入并回读验证同步歌词的 Kid3 适配器。
- 可观察、可重试且能保留错误信息的歌词任务状态。
- 对核心查询、匹配、文件保存和写入流程的测试。

## 5. 完成标准

- 用户能够为没有本地歌词的歌曲查询并选择同步歌词。
- 候选选择能够避免明显的歌曲版本或时长误匹配。
- LRC 保存使用安全写入流程，已有歌词不会被自动覆盖。
- 标签写入前后原始音频内容未被重新编码或替换。
- Kid3 写入结果经过回读确认，失败时不会丢失 LRC。
- 在线歌词功能异常不会影响音乐库和本地播放。

达到以上标准后，再进入第三阶段：本地人声分离、WhisperX 转写对齐和歌词草稿校对。
